{
  name: product.name,
  price: product.price,
  description: product.description,
  formatted: {
    # hello: product.hello
    is_discounted?: product.is_discounted?,
    tax: product.tax,
    total: product.total
  }
}